{% extends "base.html.twig" %}

{% block title %}Snowtricks - Accueil{% endblock %}

{% block content %}
<!-- ======= Hero Section ======= -->
<section id="hero" class="d-flex align-items-center">
    <div class="container text-center position-relative">
        <h1>Snowtricks</h1>
        <h2>La communauté des passionnés</h2>
    </div>
    <a href="#" class="scroll-down"><span class="iconify" data-icon="ant-design:arrow-down-outlined" data-width="30" data-height="30"></span></a>
</section>
<section>
    <div class="container">
        <!-- <div class="col-lg-12 d-flex justify-content-center">
            <ul id="portfolio-flters">
                <li data-filter="*" class="filter-active">Tout</li>
                <li data-filter=".filter-beauty">Beauté</li>
                <li data-filter=".filter-deco">Déco</li>
                <li data-filter=".filter-wedding">Mariage</li>
                <li data-filter=".filter-baby">Bébé</li>
            </ul>
        </div> -->
        <div id="trick-list" class="row justify-content-around">
            {% for trick in tricks %}
            <div class="card mt-5" style="width: 18rem;">
                <img class="card-img-top" src="../img/trick1.jpg" alt="">
                <div class="card-body d-flex">
                  <h5 class="card-title">{{ trick.name }}</h5>
                  {% if app.user %}
                    <i class="fas fa-pencil-alt"></i>
                    <i class="fas fa-trash-alt"></i>
                  {% endif %}
                  <a href="/trick/detail/{id}" class="btn btn-custom mt-0">Détail</a>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>
<!-- End Hero -->
{% endblock %}
